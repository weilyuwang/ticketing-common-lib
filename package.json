{
  "name": "@wwticketing/common",
  "version": "1.0.1",
  "description": "Common library",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "Weilyu Wang",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^3.9.5"
  }
}
